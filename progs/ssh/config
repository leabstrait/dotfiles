# Global settings for all platforms
IgnoreUnknown UseKeychain
AddKeysToAgent ask

# Include your work configurations (must be outside/before Match blocks)
Include ~/.ssh/conf.d/work.config

# macOS-specific configuration
Match exec "uname | grep -q Darwin"
    UseKeychain yes
