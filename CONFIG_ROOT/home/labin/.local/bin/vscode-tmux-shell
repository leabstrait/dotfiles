#!/bin/zsh

SESSION="vscode - $(pwd)"
WORKDIR=$(pwd)
{
    tmux attach-session -d -t "$SESSION" -c "$WORKDIR" ||
        exec tmux new-session -s "$SESSION" -c "$WORKDIR" && exit
} >/home/labin/vscode-tmux-shell.log 2>&1
